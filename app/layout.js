import './globals.css';
export const metadata = { title: 'Oakline Trend Hunter', description: 'Live trend & product research' };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
